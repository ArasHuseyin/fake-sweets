import { defineConfig } from 'astro/config'

// Domain hier eintragen, sobald sie steht – wird für SEO (canonical, OG) genutzt.
const SITE = 'https://zuckerwerk.shop'

export default defineConfig({
  site: SITE,
})
